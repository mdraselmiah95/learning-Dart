# Code Style Rules

- Every code line must have an inline comment explaining it for a beginner
- Use `// →` to show output: `print(1 + 1); // → 2`
- Group related lines under a section header: `// ── SECTION NAME ──`
- Keep examples simple and relatable (names, scores, fruits etc.)
- Prefer explicit types over `var` in examples (more educational)
