# Security Review Skill

Auto-check Dart files for common beginner mistakes:

- `!` force-unwrap on nullable without null check
- `dynamic` used where a specific type is known
- Missing `await` on async calls
- Unused variables that may cause logic errors
